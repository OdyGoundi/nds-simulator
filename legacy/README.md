# legacy/

Notebook-era predecessors kept for reference. The live application is `app/nlds_app.py`; nothing here is on the runtime path.

- `nds_app.py` — early single-file Streamlit app
- `app_lorenz.py` — minimal Lorenz-only Streamlit demo
- `in_out/` — early I/O and UI helpers
- `plotting/` — early plotting helpers (superseded by `app/plotting/`)
